# Hiawatha proxy configuration for email service
# Add this to your VirtualHost configuration in /etc/hiawatha/hiawatha.conf

# Proxy requests to /send-email to the Node.js backend
ReverseProxy /send-email http://localhost:3001/send-email
